// ltl-portfolio-stats/main.ltl
//
// Sharpe / Sortino / max-drawdown portfolio statistics.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-portfolio-stats v0.1.0")
    io::println("Sharpe / Sortino / max-drawdown portfolio statistics.")
    0
}
